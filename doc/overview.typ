#import "TypstTemplate/lib.typ": *

#show: formal

#set document(
  title: "Schema\n基于复旦万事通的智能行程规划与问答系统",
  author: "夏澄奕 24302010011",
  date: datetime.today(),
)

#title()

= 项目背景
随着校园信息量的激增，学生在学习与生活中经常需要跨越多个系统查询课程安排、设施空闲及地理位置，面临极大的“信息摩擦”与效率痛点。现有的校园查询系统往往是静态且孤立的，缺乏对空间属性（如是否配备充电插座）的精细化刻画，更无法结合学生个人的时间约束（如已选课表）进行动态的行程优化。

= 系统愿景与核心价值
本项目旨在设计一个以关系型数据库为底层核心的智能问答与轨迹规划系统。有别于传统的结构化检索工具，本系统致力于实现从“人找信息”到“系统智能调度”的跨越，其核心价值体现在以下三个维度：

- *空间与事件的集中调度*：打破“建筑-设施”与“课程-活动”的数据壁垒，通过统一的排期中枢（Allocation）实现时空资源的高效聚合。
- *基于上下文的智能规划*：系统能够动态读取用户的课表与活动报名状态，自动规避忙碌时段，并结合用户偏好（如需使用插座的自习室）推荐最优的校园行动方案。
- *约束驱动的路径求解*：降低手动寻找空间的成本，用户只需输入一组任务的时间窗、持续时长与房间类型约束，系统即可在可用房间和用户偏好上进行组合求解。

#figure(
  image("asset/interaction_flow.svg"),
  caption: [复旦百事通系统总体交互与工作流程],
)<interaction_flow>

= 核心功能与业务场景
系统围绕普通用户与管理员双端角色，提供闭环的数据服务：

== 空间资源精细化检索
支持对校园建筑及内部设施的层级化查询。用户可按校区、设施类型（如教学楼、图书馆、咖啡店）进行筛选，并获取精确的物理属性（如容量、设备配置）。

== 智能行程冲突检测与推荐 (核心)
当前 Planning 功能实现为前端 TypeScript 约束求解器。用户在界面中添加一组任务，每个任务包含：

- *时间窗*：最早开始时间 `st`、最晚结束时间 `ed` 与持续时长 `duration`。求解器只枚举满足 `st <= start` 且 `start + duration <= ed` 的候选开始时间。
- *房间类型约束*：支持不限类型、必须包含指定类型（`RoomTypePosConstraint`）与避免指定类型（`RoomTypeNegConstraint`）。前端通过 `instanceof` 区分正向与负向约束。
- *用户偏好*：系统通过 GraphQL 读取 `Preference`，将房间、建筑、校区三个层级的偏好值纳入房间评分。

求解流程分为预处理、递归排序和结果评分三步：

#enum[
  预处理阶段对每个 `(任务, 房间)` 组合计算房间分数。分数由房间类型匹配度与用户偏好加权组成。
][
  对每个任务，求解器扫描房间可用时段的端点，用最小堆维护尚未开始的房间区间，用最大堆维护当前时刻可用且得分最高的房间。由此得到 `bestScoreRoomFrom[task]`：在不同时间起点上，可使用的最优房间和其分数。
][
  递归阶段枚举任务执行顺序和候选开始时间。每一层递归先调用 `checkFeasibility`，按最晚结束时间排序剩余任务，快速剪去必然无法完成的分支。
][
  当一条完整任务序列生成后，求解器用二分查找取得对应开始时间前最近的最优房间状态，累加房间得分与相邻任务之间的路线得分，并保留全局最高分解。
]

$
  max_(pi, s_i, r_i) cal(S)
  = w_r sum_i cal(R)(t_(pi_i), r_i)
  + w_t sum_i cal(T)(r_i, r_(i+1))
$
where
$
  "Room score: " cal(R)(t_i, r_i) = w_c cal(C)(t_i, r_i) + w_p cal(P)(r_i)
  \
  "Preference: " cal(P)(r_i) in [-1, 1]
  \
  "Travel: " cal(T)(r_i, r_(i+1))
$
$
  s.t. \
  forall i, l_i <= s_i
  \
  forall i, s_i + d_i <= u_i
  \
  forall i, r_i in A_i(s_i, s_i + d_i)
$

目前界面已经将该算法接入 Planning 页面：它通过 Apollo Client 读取 `listRoom` 与登录用户的 `listPreference`，将表单输入转换为 `Task` 和 `Constraint` 对象，调用 `solve(...)` 后渲染最优任务顺序、开始时间、房间和总分。在没有单独的可用时间接口之前，前端暂时将所有载入房间视为在任务时间窗内可用；后续可直接用 `rel_Allocation` 生成的空闲时段替换该输入。

== 数据统计与后台管理
为系统管理员提供可视化的数据维护接口，支持校园排期数据的批量导入，并利用聚合查询（Aggregation）监控高频查询的校园资源，分析空间利用率与热门活动趋势。

= 数据库设计与技术路线
本系统强依赖于底层数据库的严密性与规范化设计，技术栈采用轻量化与高性能结合的 Web 架构：

- *后端中枢 (FastAPI)*：基于 Python 构筑高性能 API 接口，负责承载时空调度算法与并发请求 。
- *持久化层 (SQLite)*：采用关系型数据库保证业务数据的强一致性 。通过引入 `rel_Enrollment` (选课) 与 `rel_Allocation` (排期) 等关联实体，完美拆解多对多关系，严格满足第三范式 (3NF)。
- *交互界面 (React + MUI)*：提供响应迅速的交互式数据可视化前端，支持管理员数据控制台、用户偏好维护与 Planning 路线求解。
- *智能动态数据库查询 (GraphQL)*：利用 GraphQL 接口为动态数据库查询提供灵活性，前端通过 Apollo Client 读取房间、排期、偏好等数据，同时避免直接传输 SQL 语句的安全风险。

#figure(
  image("asset/entity_relation_diagram.svg"),
  caption: [Entity Relationship Diagram],
)<er_diagram>
